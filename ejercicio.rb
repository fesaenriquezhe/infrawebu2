#programa ue solo imprima pares

inicio = 1
termino = 10

while inicio < termino
    if inicio % 2 == 0plo
        puts "El numero #{inicio} es par"
    else
        puts "El numero #{inicio} no es par"
    end
    inicio = inicio + 1
    # inicio += 1
end